language: php
php:
  - '7.4'

services:
  - docker

install:
  - composer install --prefer-dist --no-suggest

script:
  - composer ci
  - ./docker/build.sh compiler:latest
  - ./docker/test.sh compiler:latest

cache:
  directories:
  - "$HOME/.composer/cache/files"
